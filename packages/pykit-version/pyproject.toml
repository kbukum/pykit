[project]
name = "pykit-version"
description = "Version information and compatibility"
version = "0.1.0"
readme = "README.md"
license = { text = "MIT" }
keywords = ["version", "compatibility", "semver"]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pykit_version"]
