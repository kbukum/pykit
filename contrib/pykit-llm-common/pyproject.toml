[project]
name = "pykit-llm-common"
description = "Common utilities shared across pykit LLM providers"
version = "0.1.0"
license = { text = "MIT" }
requires-python = ">=3.13"
dependencies = [
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pykit_llm_common"]
